name: Build and push multi-arch container image

on:
  workflow_call:
    inputs:
      registry:
        required: true
        type: string
      registry_user:
        required: true
        type: string
      image_name:
        required: true
        type: string
      reproduce:
        required: true
        type: boolean
      sign:
        required: true
        type: boolean
      fill_cache:
        required: false
        default: false
        type: boolean
      key_name:
        required: false
        type: string
        default: "dangerzone-tests"
    secrets:
      registry_token:
        required: true
    outputs:
      image_uri:
        description: "The published container image location, with the tag and checksum"
        value: ${{ jobs.merge.outputs.image_uri }}

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          python-version: "3.13"
          enable-cache: true

      - name: Verify that the Dockerfile matches the committed template and params
        run: |-
          cp Dockerfile Dockerfile.orig
          make Dockerfile
          diff Dockerfile.orig Dockerfile

  prepare:
    runs-on: ubuntu-latest
    outputs:
      debian_archive_date: ${{ steps.params.outputs.debian_archive_date }}
      source_date_epoch: ${{ steps.params.outputs.source_date_epoch }}
      image: ${{ steps.params.outputs.full_image_name }}
      tag: ${{ steps.params.outputs.tag }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Compute image parameters
        id: params
        run: |
          DEBIAN_ARCHIVE_DATE=$(date -u +'%Y%m%d')
          SOURCE_DATE_EPOCH=$(date -u -d ${DEBIAN_ARCHIVE_DATE} +"%s")
          TAG=${DEBIAN_ARCHIVE_DATE}-$(git describe --long --first-parent | sed 's/^v//')
          FULL_IMAGE_NAME=${{ inputs.registry }}/${{ inputs.image_name }}:${TAG}
          echo "debian_archive_date=${DEBIAN_ARCHIVE_DATE}" >> $GITHUB_OUTPUT
          echo "source_date_epoch=${SOURCE_DATE_EPOCH}" >> $GITHUB_OUTPUT
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "full_image_name=${FULL_IMAGE_NAME}" >> $GITHUB_OUTPUT
          echo "buildkit_image=${BUILDKIT_IMAGE}" >> $GITHUB_OUTPUT

  build:
    name: Build ${{ matrix.platform.name }} image
    runs-on: ${{ matrix.platform.runs-on }}
    needs:
      - prepare
    outputs:
      debian_archive_date: ${{ needs.prepare.outputs.debian_archive_date }}
      source_date_epoch: ${{ needs.prepare.outputs.source_date_epoch }}
      image: ${{ needs.prepare.outputs.image }}
      tag: ${{ needs.prepare.outputs.tag }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - runs-on: "ubuntu-24.04"
            name: "linux/amd64"
          - runs-on: "ubuntu-24.04-arm"
            name: "linux/arm64"
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Prepare
        run: |
          platform=${{ matrix.platform.name }}
          echo "PLATFORM_PAIR=${platform//\//-}" >> $GITHUB_ENV

      - name: Login to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ inputs.registry_user }}
          password: ${{ secrets.registry_token }}

      - name: Build and push by digest
        id: build
        uses: freedomofpress/repro-build@b4c783c4458bbd0ee430f516e0ed3a60108e61da # 1.0.0
        with:
          context: ./src/
          file: Dockerfile
          platforms: ${{ matrix.platform.name }}
          buildkit_image: ${{ needs.prepare.outputs.buildkit_image }}
          source_date_epoch: ${{ needs.prepare.outputs.source_date_epoch }}
          push: true
          outputs: type=image,"name=${{ inputs.registry }}/${{ inputs.image_name }}",push-by-digest=true,push=true,name-canonical=true
          build-args: DEBIAN_ARCHIVE_DATE=${{ needs.prepare.outputs.debian_archive_date }}
          annotations: rocks.dangerzone.debian_archive_date=${{ needs.prepare.outputs.debian_archive_date }}
          cache: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Export digest
        run: |
          mkdir -p ${{ runner.temp }}/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "${{ runner.temp }}/digests/${digest#sha256:}"
          echo "Image digest is: ${digest}"

      - name: Upload digest
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: digests-${{ env.PLATFORM_PAIR }}
          path: ${{ runner.temp }}/digests/*
          if-no-files-found: error
          retention-days: 1
          # Pass overwrite here as we've seen cases where the CI failed to get
          # the previous uploaded digest back.
          overwrite: true

  merge:
    runs-on: ubuntu-latest
    needs:
      - build
    outputs:
      debian_archive_date: ${{ needs.build.outputs.debian_archive_date }}
      source_date_epoch: ${{ needs.build.outputs.source_date_epoch }}
      image: ${{ needs.build.outputs.image }}
      image_uri: ${{ needs.build.outputs.image }}@${{ steps.image.outputs.digest_root }}
      tag: ${{ needs.build.outputs.tag }}
      digest_root: ${{ steps.image.outputs.digest_root }}
      digest_amd64: ${{ steps.image.outputs.digest_amd64 }}
      digest_arm64: ${{ steps.image.outputs.digest_arm64 }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Download digests
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          path: ${{ runner.temp }}/digests
          pattern: digests-*
          merge-multiple: true

      - name: Login to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ inputs.registry_user }}
          password: ${{ secrets.registry_token }}

      - name: Create manifest list and push
        working-directory: ${{ runner.temp }}/digests
        run: |
          DIGESTS=$(printf '${{ needs.build.outputs.image }}@sha256:%s ' *)
          # NOTE: The following command does NOT actually add annotations to the root manifest yet.
          # See https://github.com/freedomofpress/dangerzone/issues/1270#issuecomment-3382134379
          docker buildx imagetools create \
              --annotation "manifest-descriptor:rocks.dangerzone.debian_archive_date=${{ needs.build.outputs.debian_archive_date }}" \
              -t ${{ needs.build.outputs.image }} \
              ${DIGESTS} || \
          # In case of failures, try to --append instead \
          docker buildx imagetools create \
              --append \
              --annotation "manifest-descriptor:rocks.dangerzone.debian_archive_date=${{ needs.build.outputs.debian_archive_date }}" \
              -t ${{ needs.build.outputs.image }} \
              ${DIGESTS}

      - name: Inspect image
        id: image
        run: |
          # Inspect the image
          docker buildx imagetools inspect ${{ needs.build.outputs.image }}
          docker buildx imagetools inspect ${{ needs.build.outputs.image }} --format "{{json .Manifest}}" > manifest

          # Calculate and print the digests
          digest_root=$(jq -r .digest manifest)
          digest_amd64=$(jq -r '.manifests[] | select(.platform.architecture=="amd64") | .digest' manifest)
          digest_arm64=$(jq -r '.manifests[] | select(.platform.architecture=="arm64") | .digest' manifest)

          echo "The image digests are:"
          echo "  Root: $digest_root"
          echo "  linux/amd64: $digest_amd64"
          echo "  linux/arm64: $digest_arm64"

          # NOTE: Set the digests as an output because the `env` context is not
          # available to the inputs of a reusable workflow call.
          echo "digest_root=$digest_root" >> "$GITHUB_OUTPUT"
          echo "digest_amd64=$digest_amd64" >> "$GITHUB_OUTPUT"
          echo "digest_arm64=$digest_arm64" >> "$GITHUB_OUTPUT"

  # This step calls the container workflow to generate provenance and push it to
  # the container registry.
  provenance:
    needs:
      - merge
    strategy:
      matrix:
        manifest_type:
          - root
          - amd64
          - arm64
    permissions:
      actions: read # for detecting the Github Actions environment.
      id-token: write # for creating OIDC tokens for signing.
      packages: write # for uploading attestations.
    # NOTE: The SLSA generator requires a tag ref (not a SHA) for builder
    # identity verification. Pinning to a SHA breaks provenance generation.
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.1.0
    with:
      digest: ${{ needs.merge.outputs[format('digest_{0}', matrix.manifest_type)] }}
      image: ${{ needs.merge.outputs.image }}
      registry-username: ${{ inputs.registry_user }}
    secrets:
      registry-password: ${{ secrets.registry_token }}

  # This step ensures that the image is reproducible
  check-reproducibility:
    if: ${{ inputs.reproduce }}
    needs:
      - merge
    runs-on: ${{ matrix.platform.runs-on }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - runs-on: "ubuntu-24.04"
            name: "linux/amd64"
            arch: "amd64"
          - runs-on: "ubuntu-24.04-arm"
            name: "linux/arm64"
            arch: "arm64"
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Reproduce and verify image
        uses: freedomofpress/repro-build/verify@e3539de5c53c198f897fe6053ec2662f8f0e9bf3 # 1.1.0
        with:
          target_image: ${{ needs.merge.outputs.image }}@${{ needs.merge.outputs[format('digest_{0}', matrix.platform.arch)] }}
          file: Dockerfile
          context: ./src/
          platforms: ${{ matrix.platform.name }}
          runtime: docker
          source_date_epoch: ${{ needs.merge.outputs.source_date_epoch }}
          build-args: DEBIAN_ARCHIVE_DATE=${{ needs.merge.outputs.debian_archive_date }}
          buildkit_image: moby/buildkit:latest
          annotations: rocks.dangerzone.debian_archive_date=${{ needs.merge.outputs.debian_archive_date }}

  sign:
    if: ${{ inputs.sign }}
    runs-on: "ubuntu-latest"
    env:
      # The password of the testing key is "password"
      COSIGN_PASSWORD: "password"
      # Do not ask for validation when signing.
      COSIGN_YES: true
    needs:
      - merge
    outputs:
      image_uri: ${{ needs.merge.outputs.image_uri }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
      - name: Prepare env
        run: |-
          echo "IMAGE_URI=${{ inputs.registry }}/${{ inputs.image_name }}:${{ needs.merge.outputs.tag }}@${{ needs.merge.outputs.digest_root }}" >> $GITHUB_ENV

      - name: Install Cosign
        uses: sigstore/cosign-installer@faadad0cce49287aee09b3a48701e75088a2c6ad # v4.0.0
        with:
          cosign-release: "v2.5.0"
      - name: Check install
        run: cosign version

      - name: Login to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ inputs.registry_user }}
          password: ${{ secrets.registry_token }}

      - name: Sign container
        run: |-
          cosign sign --recursive -d --key=${{ inputs.key_name }}.key "${{ env.IMAGE_URI }}"
        shell: bash

  retag-latest:
    if: ${{ startsWith(inputs.image_name, 'dangerzone-testing/') }}
    runs-on: ubuntu-latest
    needs:
      - merge
    steps:
      - name: Login to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ inputs.registry_user }}
          password: ${{ secrets.registry_token }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Install Cosign
        uses: sigstore/cosign-installer@faadad0cce49287aee09b3a48701e75088a2c6ad # v4.0.0
        with:
          cosign-release: "v2.5.0"

      - name: Set base image
        run: |
          image_with_tag="${{ needs.merge.outputs.image }}"
          # Only keep the image name (strip the part after ":"
          echo "BASE_IMAGE=${image_with_tag%:*}" >> "$GITHUB_ENV"

      - name: Rotate or seed :old-latest
        run: |
          # Rotate :old-latest only when the previous :latest differs from the
          # new digest. Reproducible builds can produce the same digest across
          # runs; rotating in that case would collapse :latest and :old-latest
          # onto the same manifest and break the sandbox upgrade E2E test.
          #
          # For brand-new feature branches with no :latest yet, seed :old-latest
          # by copying dangerzone-testing/main/v1:old-latest (signatures included)
          # so the upgrade test has a realistic prior version to upgrade from.
          docker buildx imagetools inspect "$BASE_IMAGE:latest" --format '{{json .Manifest}}' > manifest || true
          if [ -s manifest ]; then
            previous_digest=$(jq -r .digest manifest)
            new_digest="${{ needs.merge.outputs.digest_root }}"
            if [ "$previous_digest" != "$new_digest" ]; then
              docker buildx imagetools create -t "$BASE_IMAGE:old-latest" "$BASE_IMAGE@${previous_digest}"
            else
              echo "Previous :latest already matches new digest ($new_digest); leaving :old-latest untouched."
            fi
          else
            MAIN_BASE=$(echo "$BASE_IMAGE" | sed -E 's|(dangerzone-testing)/[^/]+/v1|\1/main/v1|')
            if [ "$MAIN_BASE" = "$BASE_IMAGE" ]; then
              echo "No :latest on $BASE_IMAGE and this IS the main image path; nothing to seed."
            else
              echo "No :latest on $BASE_IMAGE; seeding :old-latest from $MAIN_BASE:old-latest"
              if ! cosign copy --force "$MAIN_BASE:old-latest" "$BASE_IMAGE:old-latest"; then
                echo "ERROR: could not seed :old-latest from $MAIN_BASE:old-latest." >&2
                echo "Ensure main's testing image has rotated :old-latest at least once." >&2
                exit 1
              fi
            fi
          fi

      - name: Update latest tag
        run: |
          docker buildx imagetools create \
            -t "$BASE_IMAGE:latest" \
            "$BASE_IMAGE@${{ needs.merge.outputs.digest_root }}"
